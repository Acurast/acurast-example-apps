{
  "name": "app-benchmark-nodejs",
  "version": "0.0.1",
  "description": "Benchmark a Node.js workload in the native Acurast runtime (compare against app-benchmark-cargo)",
  "main": "dist/bundle.js",
  "scripts": {
    "start": "npm run build && node ./dist/bundle.js",
    "build": "webpack",
    "bundle": "npm run build",
    "deploy": "npm run bundle && acurast deploy"
  },
  "author": "Andreas Gassmann <andreas@acurast.com>",
  "license": "MIT",
  "devDependencies": {
    "@acurast/cli": "^0.9.0",
    "@types/node": "^20.14.0",
    "ts-loader": "^9.5.1",
    "typescript": "^5.4.5",
    "webpack": "^5.91.0",
    "webpack-cli": "^5.1.4"
  }
}
